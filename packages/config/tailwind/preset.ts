import type { Config } from "tailwindcss";

/** Base Tailwind preset — extended by @practice-exam/ui with DESIGN.md tokens */
export const preset: Partial<Config> = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default preset;
